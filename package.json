{
  "dependencies": {
    "concurrently": "^7.6.0"
  },
  "scripts": {
    "start": "node api/server.js",
    "build": "cd api && npm install && cd ../client && npm install && npm run build",
    "dev": "concurrently \"cd client && npm start\" \"cd api && nodemon server\""
  }
}
